#include <stdio.h>

int main () {
	
	int x; // ---> Definimos una variable que se llama x de tipo int (numeros enteros)
	int y;
	float pi; // ---> Definimos una variable de tipo float (coma flotante)
	int suma;
	x=3;
	y=5;
	// 3=x MAL!!!!
	suma=x+y; 
	printf ("x=%d e y=%d\n",x,y) ; 
	printf ("La suma es = %d\n",suma);
	return 0;
}
